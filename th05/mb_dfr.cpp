// items_render() was the whole of this segment's dump contribution — the tail
// `include` was also its only content — so it goes FIRST here and the root
// contribution becomes empty (kb/codegen/0114).
#include "th04/main/item/render.cpp"
#include "th04/main/midboss/defeat_render.cpp"
