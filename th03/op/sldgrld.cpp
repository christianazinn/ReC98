// [select_cdg_reload_selected()] lives in OP's existing phase reserve in
// op_01.cpp. Keep this 8.3-safe build-graph leaf empty so the generated
// fallback graph remains stable across every TH03 profile.
