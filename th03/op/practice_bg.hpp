// Practice Setup overlays the retained 1P-vs-CPU selection scene between
// these calls. The first renders to the hidden page; the second publishes it
// and prepares the other page for the next complete frame.
void far select_vs_cpu_practice_background_put(void);
void far select_vs_cpu_practice_frame_finish(void);
